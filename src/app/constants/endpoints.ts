export const API_PREFIX = '/api/v1';

export const EMPLOYEES = {
  BASE: `${API_PREFIX}/employees`,
  IMPORT: `${API_PREFIX}/employees/import`,
  EXPORT: `${API_PREFIX}/employees/export`,
};

export const ATTENDANCE = {
  BASE: `${API_PREFIX}/attendance`,
};
